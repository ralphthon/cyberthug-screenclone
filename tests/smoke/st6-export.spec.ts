import { test } from '@playwright/test';

test.describe('ST-6: Export & Download', () => {
  test.skip('ST-6.1 download ZIP after iteration', async () => {
    // Requires at least one completed iteration with generated files
  });

  test.skip('ST-6.2 standalone HTML renders in browser', async () => {
    // Requires downloaded ZIP with generated HTML
  });
});
