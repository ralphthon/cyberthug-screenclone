{
  "name": "screenclone-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test harness and PRD for ScreenClone (RalphTon)",
  "scripts": {
    "test:smoke": "npx playwright test tests/smoke/",
    "test:smoke:ui": "npx playwright test tests/smoke/ --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
